// src/pages/TenantAdminBookingTab.tsx
// Booking module configuration tab rendered inside TenantAdmin.
// Keeping this in its own file prevents TenantAdmin.tsx from growing further.
//
// Step 2 will add: availability schedule setup (service + weekly slots)
// Step 3 will add: tenant booking slug + payment provider selection

export default function TenantAdminBookingTab() {
  return (
    <div>
      {/* Content added in Step 2 (availability setup) */}
    </div>
  )
}
